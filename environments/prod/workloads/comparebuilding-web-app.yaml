# Generated for future prod activation.
# Keep environments/prod/workloads/kustomization.yaml empty while single-cluster safety mode is active.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: comparebuilding-web-prod
  namespace: argocd
spec:
  project: comparebuilding-web
  source:
    repoURL: https://github.com/wlodzimierrr/homelab-workloads.git
    targetRevision: main
    path: apps/comparebuilding-web/envs/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: comparebuilding-web
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
