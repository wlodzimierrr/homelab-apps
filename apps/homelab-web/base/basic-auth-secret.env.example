# htpasswd user entries consumed by Traefik basicAuth middleware.
# Generate with:
#   htpasswd -nbB admin 'change-me-now'
# Then store the generated line exactly as the value below.
users=admin:$2y$05$replace_with_htpasswd_hash
